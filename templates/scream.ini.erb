; Enable 'Break the silence operator' extension module
extension=scream.so

;  Runtime configuration
;  http://php.net/manual/en/scream.configuration.php

;  Whether or not to enable scream.
scream.enabled=<%= @enabled %>
